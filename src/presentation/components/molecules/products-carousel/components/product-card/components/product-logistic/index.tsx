import React from 'react';
import { LogisticContainer, LogisticText } from './style';

const ProductLogistic = () => {
  return (
    <LogisticContainer>
      <LogisticText>Recibe Hoy</LogisticText>
    </LogisticContainer>
  );
};

export default ProductLogistic;
