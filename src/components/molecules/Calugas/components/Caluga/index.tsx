import { CalugaItemProps } from '../../Calugas.types';

const Caluga = (props: CalugaItemProps) => {
  const { image, description, title, link, width } = props;

  return <></>;
};

export default Caluga;
