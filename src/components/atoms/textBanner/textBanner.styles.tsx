import styled, { css } from "styled-components";

export const TextBannerContainer = styled.div`
    width: 100%;
    max-height: 50px;
    height: 50px;
    cursor: pointer;

    img{
        object-fit: fill;
        width: 100%;
        height: 100%;
    }
`;
