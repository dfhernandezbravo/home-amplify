import styled, { css } from "styled-components";

export const TextBannerContainer = styled.div`
    width: 100%;
    height: 40px;
    cursor: pointer;
    
    img{
        object-fit: fill;
        width: 100%;
        height: 100%;
        max-height: 40px;
    }
`;
