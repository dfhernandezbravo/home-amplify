import styled, { css } from "styled-components";

export const TextBannerContainer = styled.section`
    width: 100%;
    height: fit-content;
    cursor: pointer;
    margin-bottom: -5px;
    
    img{
        object-fit: fill;
        width: 100%;
        height: auto;
    }
`;
